1. Установить php, затем сделать доступным файл php.ini-development, оставив в названии расширения только ini, затем в переменные окружения добавить в Path путь в папку с разархивированным php.
	Далее, запустить терминал в папке с tasks, прописать php -S localhost:8000, затем перейти по ссылке.

2. БД на сиквеле с одной таблицей, необходимой по тз, динамическая таблица работающая через get запросы , обращающаяся к бд; сортировка по 4 категориям, и создание задачи через post; внешний вид подогнал под bitrix-компоненты.

3. дня 2 заняла задача.

4. как инициализировать пхп на десктопе; в пхп тоже можно работать по ООП, так и сделал тз; узнал про синтаксис пхп, напоминает джс